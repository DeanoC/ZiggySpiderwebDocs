
All capabilities are exposed through the namespace.

There are no parallel integration primitives.

If a resource cannot be expressed as part of the filesystem, it does not belong in the system.

This ensures:

- Uniform discovery
- Uniform permission handling
- Uniform introspection
- Uniform composition

Capabilities are mounted, not injected.