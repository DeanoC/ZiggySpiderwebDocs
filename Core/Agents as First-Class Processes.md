Agents are not external orchestrators.

They are processes operating inside the same namespace as all other resources.

They do not require privileged integration layers.

They operate using the same primitives as everything else.
